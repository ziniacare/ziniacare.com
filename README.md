# ziniacare.com

The ZiniaCare AB website — a single self-contained static page. No build step,
no dependencies, no JS: `index.html` is the entire site. Deploy = push to `main`.

The ZiniaCare System is under development — not yet CE marked, not available
for sale. Page copy is deliberately limited to verifiable company facts.
